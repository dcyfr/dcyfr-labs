# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# ========================================
# Build & Output
# ========================================
# Next.js
/.next/
/out/

# Production
/build

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Cached build-time data
/public/data/*.json
!/public/data/.gitkeep

# ========================================
# Dependencies
# ========================================
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/cache
!.yarn/sdk

# Package manager caches
.npm/
.pnpm-store/
.yarn/cache/
.yarn/install-state.gz

# ========================================
# Testing & Quality
# ========================================
# Test coverage and results
/coverage/*
/test-results/*
/scripts/reports/*
/scripts/__tests__/*
/.playwright-mcp/*
/playwright-report/*
/playwright/.cache/
/lighthouse/
/.lighthouseci/

# Test output logs
test-output.log
**/e2e-test-results.log
**/*-test-results.log

# Security scanning results
nuclei-results.json
nuclei-report.md
nuclei-*.json
nuclei-*.md
!nuclei-integration.md

# Playwright visual regression & test artifacts
# These contain screenshots/videos/diffs generated during CI or local runs
reports/e2e-artifacts/**
e2e/**-spec.ts-snapshots/**
**/*-spec.ts-snapshots/**
**/*.spec.ts-snapshots/**

# -----------------------------------------------------------------------------
# Allowlist: Enable committing *sanitized* test reports when appropriate
# - To re-allow committing already-generated HTML/text reports for historical
#   preservation or release notes, explicitly un-ignore the directories below.
# - DO NOT commit raw screenshots or videos (they may contain sensitive content).
# - Scan any report artifacts with `npm run scan:pi` before committing.
# -----------------------------------------------------------------------------
!/coverage/
!/test-results/
!/playwright-report/

# ========================================
# Environment & Secrets
# ========================================
# Private directories (at any level)
# Note: Use .private/ for new directories (dot-prefix convention)
**/.private/
**/private/
.private/
private/
!/src/app/private/

# Archive directories (at any level)
# Note: Use .archive/ for new directories (dot-prefix convention)
**/.archive/
**/archive/
.archive/
archive/

# env files
.env*
!.env.example
!.env.template

# API keys and tokens
*.key
*.token
*.secret

# ========================================
# AI Agent Configurations
# ========================================
.github/agents/*
!.github/agents/*.md
!.github/agents/enforcement/
!.github/agents/instructions/
!.github/agents/learning/
!.github/agents/patterns/

# AI assistant configurations (local overrides)
.github/copilot-instructions.local.md
.github/copilot-instructions.*.md
!.github/copilot-instructions.md

# Claude Desktop configs (local)
.claude/
.agent/
claude_desktop_config.json

# MCP (Model Context Protocol) configs
.mcp.json
mcp.json

# local configs
.anthropic/
.openai/
.opencode.config.json

# AI Agent Session State (local only - exclude all session state files)
*.session-state.json
.opencode/.session-state.json
.claude/.session-state.json
.github/copilot-session-state.json

# Session checkpoints (auto-generated, keep structure)
.git/agent-checkpoints/*.json
.git/agent-checkpoints/.auto-checkpoint-*.pid

# OpenCode cache and temporary files
.opencode/.cache/
.opencode/.sessions/

# OpenCode dependencies (local node_modules)
.opencode/node_modules/

# OpenCode skills (downloaded skill files)
skills/

# AI cost archives (historical data snapshots)
.ai-costs-archive/

# IDE & Editor Configs
# ========================================
# VS Code (allow workspace settings, block user overrides)
.vscode/*
!.vscode/mcp.json
!.vscode/mcp.json.example
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/design-tokens.code-snippets
.vscode/*.local.json
.vscode/*.local

# JetBrains IDEs
.idea/
*.iml
*.iws

# Other editors
.vs/
.fleet/
.windsurf/

# ========================================
# Development & Debug
# ========================================
# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log.local

# Local development overrides
*.local
*.local.*
.local/
*-local.*

# Debug outputs
.debug/
debug/
!debug/.gitignore
!scripts/debug/check-images.mjs
dump.rdb

# Performance profiling
*.cpuprofile
*.heapprofile
*.heapsnapshot
.clinic/

# ========================================
# Platform & Deployment
# ========================================
# Vercel
.vercel
!.vercel/README.md
.vercel.json.local

# Sentry
.env.sentry-build-plugin
.sentryclirc

# ========================================
# Temporary & Cache Files
# ========================================
# Cache directories
.cache/
.temp/
.tmp/
tmp/

# OS-specific
.DS_Store
*.pem
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
.Spotlight-V100
.Trashes

# Local backup files
*.bak
*.old
*.orig
*.save
*.backup
*.backup.*

# Report artifacts (generated, not source)
gitleaks-report*.json
test-results.json
reports/prose-*

