/**
 * ESLint Flat Config - dcyfr-labs
 * 
 * TEMPORARY: Minimal config while diagnosing configuration loading issue
 * All rules and patterns will be re-added once ESLint load error is resolved
 */

export default [
  {
    ignores: ["node_modules/**", ".next/**", "build/**", "dist/**"],
  },
];
