/**
 * Loading state for projects page.
 * DISABLED: Returns null to prevent skeleton containers from showing during load.
 * The page loads fast enough that skeleton states create visual noise.
 * 
 * @see src/app/projects/page.tsx
 */
export default function Loading() {
  return null;
}
