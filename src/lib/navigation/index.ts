/**
 * Navigation Module
 * 
 * Centralized navigation configuration and utilities
 * 
 * @module
 */

// Export types
export * from "./types";

// Export config (which includes re-exported utilities)
export * from "./config";

// Note: Utils are already re-exported from config.ts to avoid duplication
