{
  "emailDomains": [
    "github.com",
    "example.com",
    "dcyfr.ai"
  ],
  "emails": [],
  "paths": [
    "docs/**",
    "docs/operations/scanner-tests-completed.md",
    "scripts/.pii-allowlist.json"
  ],
  "piiPaths": [],
  "piPaths": [
    "src/app/globals.css",
    "src/lib/design-tokens.ts",
    "src/**"
  ],
  "proprietaryPaths": [
    "docs/ai/logging-security.md"
  ],
  "privateKeyPaths": [],
  "allowlistReasons": {
    "src/app/globals.css": "CSS design tokens and property names (e.g., 'order', 'border', 'oklch') are benign style tokens and may trigger conservative PI pattern matches.",
    "src/lib/design-tokens.ts": "Design token keys and example strings (e.g., 'border-*') are semantic tokens, not transaction/order identifiers.",
    "docs/": "Documentation includes example emails and placeholders which are expected and safe.",
    "docs/**": "Documentation and example reports may include placeholder data that triggers conservative PI/PII patterns; these entries are safe when used for docs and examples.",
    "src/**": "Source files may include example strings, placeholders, or tokens that match PI/PII heuristics; use specific allowlist entries for narrower exceptions if possible.",
    "docs/operations/scanner-tests-completed.md": "Test documentation containing example phone numbers (e.g., 5551234567) for PII scanner validation purposes.",
    "scripts/.pii-allowlist.json": "PII allowlist configuration file containing example patterns for documentation and scanning reference purposes.",
    "docs/ai/logging-security.md": "AI logging/security documentation references proprietary terminology for classification examples and is expected in this context."
  }
}