# Git LFS Configuration for Visual Regression Test Snapshots
# Tracks all PNG files in test snapshot directories using Git LFS
# This prevents large binary files from bloating the repository

# Visual regression test snapshots (Playwright)
e2e/visual/__snapshots__/**/*.png filter=lfs diff=lfs merge=lfs -text
e2e/__snapshots__/**/*.png filter=lfs diff=lfs merge=lfs -text
playwright-report/**/*.png filter=lfs diff=lfs merge=lfs -text

# Component storybook snapshots (when added)
.storybook/**/*.png filter=lfs diff=lfs merge=lfs -text

# Test reports and artifacts
reports/**/*.png filter=lfs diff=lfs merge=lfs -text
test-results/**/*.png filter=lfs diff=lfs merge=lfs -text
