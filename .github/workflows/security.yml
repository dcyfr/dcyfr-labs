name: Security Checks - DEPRECATED

# ⚠️ DEPRECATION NOTICE
# This workflow has been merged into security-suite.yml
#
# Status: INACTIVE (2026-01-13)
# Reason: All npm audit functionality is now in security-suite.yml
#
# The following features from this workflow are now in security-suite.yml:
# ✅ npm audit (on PR + schedule)
# ✅ Outdated packages check
# ✅ Comprehensive security reporting
# ✅ PR comments with results
#
# DO NOT use this workflow. It will be removed in February 2026.
# Use security-suite.yml instead: .github/workflows/security-suite.yml
#
# To verify all features are in security-suite.yml:
# 1. Check security-suite.yml includes npm audit job
# 2. Check outdated packages checking is implemented
# 3. Check reporting and PR comment functionality exists
#
# Questions? See docs/automation/workflow-debugging.md

on:
  workflow_dispatch: # Manual trigger only - for testing if needed

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: ⚠️ DEPRECATED WORKFLOW
        run: |
          echo "::error::This workflow (security.yml) is DEPRECATED."
          echo "::error::Use security-suite.yml instead (.github/workflows/security-suite.yml)"
          exit 1
