# ========================================
# NPM Configuration (dcyfr-labs)
# ========================================

# Force npm usage (prevent yarn/pnpm)
engine-strict=true

# ========================================
# Private Package Registry (Optional)
# ========================================
# Uncomment to use GitHub Packages for @dcyfr scoped packages
# Requires GITHUB_PACKAGES_TOKEN in .env.local or environment
# See: docs/architecture/private-package-setup-guide.md

# @dcyfr:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${GITHUB_PACKAGES_TOKEN}
